spring:
# Database & JPA
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3308/casebefrank
    username: root
    password: password
  jpa:
    hibernate.ddl-auto: update
    generate-ddl: true
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        hibernate.format_sql: true

# Base security settings
  security:
    user:
      name: JamieOliver
      password: JamieOliver@2023

